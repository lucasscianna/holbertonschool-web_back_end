#!/usr/bin/env python3
"""Provides a function that calculates the floor of a float."""

import math


def floor(n: float) -> int:
    """Returns the floor of a floating-point number."""
    return math.floor(n)
